# 최종 회고 (Final Retrospective)

> P1 → P2 → P3 까지의 학습/개선/다음 단계 한 페이지 회고.

## 1. 프로젝트 한 줄

NestJS + React + MySQL 기반 **온라인 강의 플랫폼**.
P1: 도메인 골격 / P2: 결제·수강·진도율·운영 기능 / P3: 외부연동·관측성·CI/CD.

## 2. 배운 점

### 도메인 모델링
- 결제·수강·진도율은 처음에 한 덩어리로 보였지만 분리하니 각각의 상태기계가 또렷해졌다.
- 영상 없이 완료 처리되지 않는다 같은 작은 규칙도 BE/FE 양쪽에서 막아야 신뢰감이 생긴다.

### 외부 API 안정성
- 단순히 `fetch` 한 번이 아니라 **재시도 + 멱등성 + 백오프 + jitter** 의 조합이 기본기.
- 4xx와 5xx를 같은 catch에서 다루면 안 된다. 4xx는 즉시 실패시켜야 사용자에게 빠른 피드백이 간다.

### 관측성
- "측정 안 되는 건 고칠 수 없다" — `/metrics`, `/readyz` 만 있어도 운영 자신감이 다르다.
- 구조화 로그(JSON 한 줄)에 `x-request-id` 가 박히는 것만으로도 추적 가능성이 폭발적으로 좋아진다.

### 운영
- `runbook.md`, `incident-report-sample.md` 같은 문서를 미리 양식으로 만들어두는 게 큰 차이를 만든다 — 사고 한가운데서 문서를 새로 쓰진 못한다.

## 3. 잘한 결정 (Keep)

- 결제 confirm 에 **멱등성**을 도입 — 사용자 더블클릭/재시도/네트워크 retry 모두 안전
- 영수증 메일을 **비차단**으로 보냈다 — 메일 서버 죽어도 결제가 망가지지 않는다
- 관리자 페이지를 **공용 Navbar 와 완전히 분리** — 권한 경계가 시각적으로도 분리
- 외부 API 어댑터(`TossClient`, `NotificationsService`) 를 별도 클래스로 — 테스트에서 `overrideProvider` 만으로 격리 가능

## 4. 아쉬운 결정 (Problem)

- 초기에 `description` 같은 필드를 `findWithPagination` 의 `select` 에서 누락 — 디테일 페이지에서 빈 값이 보일 뻔. 이후 검토 라운드에서 발견.
- 진도율 추적이 슬라이더 수동 저장 방식 — YouTube IFrame Player API 로 실제 시청 시간을 잡아내면 더 자연스러웠을 것
- 단일 인스턴스 가정에 묶여 분산 락/큐 미도입 — 트래픽 증가 시 재설계가 필요

## 5. 다음 단계 (Try)

1. **자동 진도율** — YouTube IFrame Player API 를 띄워 watch time 을 자동 기록
2. **환불/취소 흐름** — Toss `cancel` API, 환불 사유 코드, 부분 환불 정책
3. **알림 다양화** — 이메일 외에 카카오 알림톡 / 슬랙(강사용) 추가, 채널 추상화 후 전략 패턴
4. **BullMQ** 도입 — 알림/리마인더가 늘면 큐가 필요해진다
5. **OpenTelemetry** — 메트릭/트레이스 표준화. 현재는 인-프로세스 카운터만 사용
6. **e2e 시나리오 비주얼** — Playwright 로 결제 위젯까지 한 번에 검증

## 6. 개인 학습 점수표

| 영역 | 시작 (P1) | 끝 (P3) |
|------|-----------|---------|
| 도메인 모델링 (TypeORM, 관계) | 2 | 4 |
| 트랜잭션/멱등성 사고 | 1 | 4 |
| 관측성 (메트릭/로그/헬스) | 1 | 3 |
| 외부 API 안전 통신 | 2 | 4 |
| 운영 문서 작성 | 1 | 3 |
| CI/CD 자동화 | 2 | 3 |

(5점 만점 자체평가)

## 7. 마지막으로

세 단계로 나누어 만든 가장 큰 효과는, "한 번에 다 만들었으면 못 봤을 작은 결정들" 이 단계 사이에 드러난 점이다.
결제 멱등성, 영상 없으면 완료 금지 같은 규칙은 "동작하는 코드" 단계에선 안 보이고 "운영하는 코드" 단계가 되어야 보인다.

다음 프로젝트는 처음부터 이 시야로 시작할 수 있을 것 같다.
